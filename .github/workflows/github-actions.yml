name: Test workflow
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run docker compose
        run: docker-compose up -d
      - name: Tests
        run: npm test